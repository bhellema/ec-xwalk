export default function decorate(block) {
  // Hero Partners block - minimal decoration
  // Block structure: Row 1 = background image, Row 2 = heading text

  // Ensure block has the hero-partners class
  block.classList.add('hero-partners');
}
